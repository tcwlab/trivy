# hadolint configuration for tcwlab/trivy
#
# DL3018: Pin versions in apk add
# Alpine images deliberately do not pin apk package versions, as they use the
# Alpine repository as source-of-truth and update only via apk upgrade.
# Version pinning would break on Alpine releases.
ignore:
  - DL3018
